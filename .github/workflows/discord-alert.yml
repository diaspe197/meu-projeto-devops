name: Alerta para o Discord

on:
  push:
    branches:
      - main

jobs:
  alerta-discord:
    runs-on: ubuntu-latest
    steps:
      - name: Enviar alerta para o Discord via curl
        run: |
          curl -H "Content-Type: application/json" \
               -X POST \
               -d "{\"content\": \"Novo push no repositório!\n👤 Autor: ${{ github.actor }}\nRepositório: ${{ github.repository }}\nBranch: ${{ github.ref }}\"}" \
               ${{ secrets.DISCORD_WEBHOOK }}
